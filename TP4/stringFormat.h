/** \brief Cambia la primer letra de una palabra a mayuscula.
 *
 * \param char[] La cadena de caracteres.
 * \return void No devuelve algo.
 *
 */
void changeFirstToUpper(char[]);

/** \brief Cambia el formato de una palabra a todo mayuscula.
 *
 * \param char[] La cadena de caracteres.
 * \return void No devuelve algo.
 *
 */
void changeStringToUpper (char[]);

/** \brief Cambia el formato de una palabra a todo minuscula.
 *
 * \param char[] La cadena de caracteres.
 * \return void No devuelve algo.
 *
 */
void changeStringToLower (char[]);

